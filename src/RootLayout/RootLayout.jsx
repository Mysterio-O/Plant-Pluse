import React from 'react';
import { ThemeProvider } from '../contexts/ThemeContext';
const RootLayout = () => {
    return (
        <ThemeProvider>
            
        </ThemeProvider>
    );
};

export default RootLayout;